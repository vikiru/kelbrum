[project]
name = "graph"
version = "0.1.0"
description = "Pure relationship graph construction and family policies"
requires-python = ">=3.13"
dependencies = ["msgspec>=0.18", "orjson>=3.10"]

[build-system]
requires = ["uv_build>=0.12.1,<0.13.0"]
build-backend = "uv_build"
