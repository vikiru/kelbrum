[project]
name = "export"
version = "0.1.0"
description = "Kelbrum frontend artifact export package"
requires-python = ">=3.13"
dependencies = ["config", "models", "processing", "storage"]

[build-system]
requires = ["uv_build>=0.12.1,<0.13.0"]
build-backend = "uv_build"
