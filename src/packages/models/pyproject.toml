[project]
name = "models"
version = "0.1.0"
description = "Kelbrum models package"
requires-python = ">=3.13"
dependencies = ["msgspec>=0.18"]

[build-system]
requires = ["uv_build>=0.12.1,<0.13.0"]
build-backend = "uv_build"
